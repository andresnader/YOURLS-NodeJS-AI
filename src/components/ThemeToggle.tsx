// Theme toggle is no longer needed — this app uses a permanent dark refractive theme.
// Keeping file to avoid import errors if referenced elsewhere.
"use client";

export function ThemeToggle() {
  // Dark-only mode — no toggle needed
  return null;
}
